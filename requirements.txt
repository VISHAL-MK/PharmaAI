# Drug AI — Python dependencies
# Install with: pip install -r requirements.txt

# Core (required)
requests>=2.31.0
pillow>=10.0.0

# Optional — improves OCR image preprocessing significantly
# Comment out if you don't want to install OpenCV
opencv-python>=4.8.0
numpy>=1.24.0

# Optional — barcode/QR decoding for batch verification module (NEW)
# Also requires system library libzbar0 (Linux) or zbar (Mac)
pyzbar>=0.1.9

# Backend integration
fastapi>=0.111.0
uvicorn>=0.23.0
pymongo>=4.5.0
bcrypt>=4.0.0
python-jose[cryptography]>=3.0.0
python-multipart>=0.0.6

# Sarvam AI — Multilingual OCR, Translation, TTS, LLM
sarvamai>=0.1.28
httpx>=0.21.2
